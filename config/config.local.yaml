service_name: gateway-service
env: local
version: dev

log_level: debug
shutdown_timeout: 10s

http:
  port: 8080
  timeout: 5s

catalog_grpc:
  addr: localhost:9091
  timeout: 2s

otlp:
  endpoint: localhost:4317
