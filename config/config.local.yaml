service_name: gateway-service
env: local
version: dev

log_level: debug
shutdown_timeout: 10s

http:
  port: 8083
  timeout: 5s

catalog_grpc:
  addr: localhost:9091
  timeout: 2s

otlp:
  endpoint: localhost:4317

auth_grpc:
  addr: localhost:44044
  timeout: 3s
